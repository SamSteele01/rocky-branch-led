function findPowerOf2FrequenciesTo(max: number, min: number) {
  for (let i = 2; i < max; i *= 2) {
    // if (i > min) {
    console.log(i);
    // }
  }
}

findPowerOf2FrequenciesTo(10000000, 100000);
